<section id="details-page">
  <h1>Details</h1>
  <article class="details-card">

    <article class="details-card-text">
      <h2>Title:<br /> {{publication.title}}</h2>
      <h3>Author: {{publication.authorUsername}}</h3>
      <h3>Painting technique: {{publication.paintingTec}}</h3>
      <h3>Certificate of authenticity: {{publication.certificate}}</h3>
      {{#if user}}
        <!-- If there is no registered user, do not display buttons-->
        <div class="buttons">
          {{#if publication.isAuthor}}
            <!-- Only for registered user and author of the publication -->
            <a href="/arts/edit/{{publication._id}}" class="btn-edit">Edit</a>
            <a
              href="/arts/delete/{{publication._id}}"
              class="btn-delete"
            >Delete</a>
          {{else}}
            <!-- logged in user who has not yet shared the publication-->

            <!-- logged in user who has already shared the publication-->
            {{#if publication.shared}}
              <p class="shared-pub">You already shared this publication</p>
            {{else}}
              <a href="/arts/share/{{publication._id}}" class="btn-share">Share
                publication</a>
            {{/if}}
          {{/if}}
        </div>
      {{/if}}

    </article>

    <article class="details-card-image">
      <img src="/static/img/img-5.jpg" alt="art-image2" />
    </article>

  </article>
</section>